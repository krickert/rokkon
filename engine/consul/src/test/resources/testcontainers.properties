# Enable container reuse to share containers across test runs
testcontainers.reuse.enable=true

# Ryuk is the resource reaper that cleans up containers
# We keep it enabled for safety but containers marked for reuse won't be cleaned
ryuk.disabled=false