# Gradle properties

# Gradle JVM settings
org.gradle.jvmargs=-Xmx4g -Xms1g -XX:MaxMetaspaceSize=1g -XX:+UseG1GC
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Quarkus versions (these are already correct)
quarkusPluginVersion=3.24.2
quarkusPlatformGroupId=io.quarkus.platform
quarkusPlatformArtifactId=quarkus-bom
quarkusPlatformVersion=3.24.2

# ADD THESE TWO LINES for grpc and assertj versions
grpc.version=1.73.0
assertj.version=3.27.3
swagger.ui.version=2.2.31