# Test: Can Quarkus run multiple gRPC servers in same JVM?

quarkus:
  grpc:
    # Main gRPC server
    server:
      port: 50051
      
    # Can we define additional servers?
    servers:
      echo-server-2:
        port: 50052
        # services:
        #   - com.rokkon.modules.echo.EchoService
      
      echo-server-3:
        port: 50053
        # services:
        #   - com.rokkon.modules.echo.EchoService
        
  # Test client configuration
  grpc:
    clients:
      echo-1:
        host: localhost
        port: 50051
      echo-2:
        host: localhost
        port: 50052
      echo-3:
        host: localhost
        port: 50053