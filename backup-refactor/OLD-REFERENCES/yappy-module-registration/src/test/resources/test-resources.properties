# Enable basic test containers for consul only
testcontainers.enabled=true
testcontainers.consul=true

# Direct property values for test-module (no container)
test-module.host=localhost
test-module.grpc.port=50051