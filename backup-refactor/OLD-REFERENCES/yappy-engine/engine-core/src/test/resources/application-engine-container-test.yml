micronaut:
  application:
    name: engine-container-test
  server:
    port: ${random.port}
  test-resources:
    enabled: true
    
# Let test resources provide all container endpoints
# No hardcoded values to ensure dynamic resolution